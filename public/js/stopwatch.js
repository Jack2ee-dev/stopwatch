let currentTime = {
  hour: 0,
  minute: 0,
  second: 0,
  millisecond: 0,
};

const timer = (() => {
  const STATUS_INIT = 'INIT';
  const STATUS_START = 'START';
  const STATUS_STOP = 'STOP';

  let currentStatus = STATUS_INIT;

	
})();
